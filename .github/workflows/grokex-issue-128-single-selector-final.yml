name: Grokex Issue 128 final source

on:
  pull_request:
    branches:
      - codex/issue-128-single-provider-selector

permissions:
  contents: read

jobs:
  final-source:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 2
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Verify product parent
        shell: bash
        run: test "$(git rev-parse HEAD^)" = "725488f569d5d467e0441901ae9e33014bb79066"

      - name: Prepare Linux Cargo environment
        uses: ./.github/actions/setup-ci

      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582
        with:
          toolchain: 1.95.0
          components: clippy,rustfmt

      - uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532
        with:
          tool: cargo-nextest

      - name: Verify authoritative formatting and config schema
        shell: bash
        working-directory: codex-rs
        run: |
          set -euo pipefail
          cp Cargo.lock "${RUNNER_TEMP}/Cargo.lock"
          cargo fmt -- --config imports_granularity=Item
          just write-config-schema
          git diff --exit-code -- core/config.schema.json
          cp "${RUNNER_TEMP}/Cargo.lock" Cargo.lock
          cd ..
          git diff --binary > "${RUNNER_TEMP}/issue-128-final-authority.patch"
          git status --short > "${RUNNER_TEMP}/issue-128-final-authority-status.txt"

      - name: Upload final authority evidence
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f
        with:
          name: issue-128-final-authority
          path: |
            ${{ runner.temp }}/issue-128-final-authority.patch
            ${{ runner.temp }}/issue-128-final-authority-status.txt

      - name: Test Provider selector and catalog contracts
        working-directory: codex-rs
        run: |
          just test -p codex-model-provider-info
          just test -p codex-model-provider
          just test -p codex-models-manager
          just test -p codex-config

      - name: Compile affected downstream contracts
        working-directory: codex-rs
        run: |
          cargo check -p codex-login --lib --tests
          cargo check -p codex-core --lib --tests
          cargo check -p codex-app-server --lib --tests

      - name: Lint changed implementation crates
        working-directory: codex-rs
        run: >-
          cargo clippy
          -p codex-model-provider-info
          -p codex-model-provider
          -p codex-config
          --lib --tests -- -D warnings
