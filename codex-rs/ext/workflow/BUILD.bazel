load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "workflow",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "codex_workflow_extension",
    integration_compile_data_extra = glob(["templates/**"]),
)
